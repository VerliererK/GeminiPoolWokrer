{
    "name": "gemini-pool-worker",
    "version": "1.0.0",
    "description": "Cloudflare Worker for Gemini API Key Pool",
    "scripts": {
        "dev": "wrangler dev",
        "deploy": "wrangler deploy",
        "tail": "wrangler tail"
    },
    "devDependencies": {
        "wrangler": "^4.14.1"
    }
}